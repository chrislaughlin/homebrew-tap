# Homebrew tap

Homebrew formulae maintained by Chris Laughlin.

```bash
brew tap chrislaughlin/tap
brew install local-ai-advisor
```

Or install directly:

```bash
brew install chrislaughlin/tap/local-ai-advisor
```
